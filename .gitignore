# Build artifacts
build/
sdkconfig
sdkconfig.old
managed_components/
dependencies.lock

# Environment
.env
*.env.local

# Node / Python dependencies (never commit these)
node_modules/
.venv/
.venv-stt/
venv/
__pycache__/
*.pyc

# Runtime state
logs/
*.log
.workbuddy/

# Local device registry (contains real MAC addresses) — copy devices.json.example
ai-server/devices.json

# Runtime state files
ai-server/scripts/.quant_broadcaster_state.json

# Large STT model weights (download separately, see ecosystem.config.js comments)
ai-server/models/faster-whisper-small/model.bin

# IDE
.vscode/
.idea/
*.swp
*.swo
.DS_Store

# Firmware binaries
*.bin
*.elf
*.hex
*.map

# Audio/media
*.mp3
*.wav
*.avi
*.mp4

# Claude/agent notes
.claude/

# Large model files (keep out of git)
*.wn9
*.wav2vec
*.model
# Reference repos (cloned, not our code)
firmware/
repos/

# Archive build artifacts (contain personal paths)
archive/*/build/
archive/*/managed_components/
archive/*/sdkconfig
archive/*/dependencies.lock

# Backups
backups/
