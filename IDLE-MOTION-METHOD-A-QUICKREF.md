# 方法 A 实操速查：关掉瓦力「空闲乱转头」（免烧录）

> 源码已核实（stackchan-fw v2.2.4 血统固件），改动仅需设备端屏幕操作，零编译零烧录。
> 持久化到设备 NVS，重启后仍生效；只关舵机转头，**保留脸上轻微表情**。

---

## 设置项长什么样（源码对齐）

固件里那个设置项的实际 UI 是：

- **标题**：`Idle movement frequency:`（位于 SETUP 应用 → **AI.Agent** 分节 → **General** 面板）
- **控件**：一个**滑杆（slider）**，从左到右四档：

| 档位 | 值 | 效果 |
|---|---|---|
| **Off** | 0 | **舵机完全不转**（要的）|
| Low | 1 | 低频随机转头 |
| Medium | 2 | 默认 |
| High | 3 | 高频随机转头 |

> 把它**拖到最左（Off）**即可。源码：`XiaozhiGeneralWorker`，标签数组
> `{"Off","Low","Medium","High"}`，滑块 `setRange(0,3)`。

---

## 操作路径

1. **从当前 AI Agent（语音助手）界面返回到主菜单（App Launcher 宫格）。**
   > 这一步的手势取决于你固件版本的 UI（通常屏幕某个方向滑动/长按退出/返回手势）。你平时切应用用哪个手势就照用。
2. 在主菜单宫格里点 **SETUP**（设置）图标进入。
3. 在 SETUP 菜单里选分节 **AI.Agent**。
4. 在 AI.Agent 下选 **General**（通用）。
5. 找到 **Idle movement frequency** 滑杆，**拖到最左 = Off**。
6. 保存/返回退出。

---

## 怎么验证生效

改完让瓦力**空闲待机十几秒**，观察：

- **头**：应保持**静止正对前方**，不再每 4–8 秒左右摇头/上下瞟。
- **脸**：眼神游离、嘴部小动等**轻微表情仍在**（由独立的 IdleExpressionModifier 驱动，不受此设置影响）。

若你还是想确凿判断"舵机是否真的没动"，最直接就是看它——转头是机械动作，肉眼即可确认，无需读日志。

---

## 如果找不到入口（回退方法 B）

免烧录的前提是你能从正常使用界面进到 SETUP 页。**如果找不到回主菜单的手势、或进不去 SETUP**，就不折腾了，走**方法 B**（改一处编译期默认值 + 我这边帮你烧录），确定性强：

- 文件 `firmware/main/hal/board/stackchan_display.h:22`：`idle_motion_level_ = 2` 改成 `0`。
- 重新编译并 `app-flash`（只烧 app 分区，保留 NVS/激活，约十几分钟）。

告诉我一声，方法 B 我可以全程执行到烧录。
